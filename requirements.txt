# Minimum versions rather than exact pins. The previous `==` pins on fastapi and
# jinja2 were never revisited and left fourteen known vulnerabilities in place, so
# each bound below is a floor that security updates can move above.
#
# jinja2 and python-multipart floors are set by published advisories; do not lower
# them. See SECURITY.md.

fastapi>=0.115.0
uvicorn>=0.32.0
jinja2>=3.1.6            # GHSA sandbox escapes and xmlattr injection, fixed in 3.1.6
python-multipart>=0.0.32 # multiple DoS and parsing advisories, fixed by 0.0.31
slowapi>=0.1.9

pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

pandas>=2.2.0
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.11.0

passlib>=1.7.4
python-jose>=3.5.0
cryptography>=44.0.0

supabase>=2.0.0
psycopg2-binary>=2.9.9
requests>=2.32.0
cachetools>=5.3.0
