# Test and lint tooling. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0.0
pytest-cov>=5.0.0
httpx>=0.27.0    # required by fastapi.testclient
ruff>=0.6.0
